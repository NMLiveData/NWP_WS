/*,
        appEntanceCounter:0,
        currentViewers:0,
        currentLightning:50,
        accumulateViewersPerDay:0,
        lastUpdate:"0",
        accumulateSinceStart:[
            {
                "date": "29/06/17",
                "count": 0
            }
        ]*/



    /*Session.findOne({photoID:id}).
        exec(function(err, doc){
            if(err){
                console.log("error: " + err);
                return 0;
            }

            doc.appEntranceCounter++;

            doc.save(function(err){
                if(err){
                    console.log(err);
                    res.json({success:false,data:err});
                } 
                else{
                    console.log("New appEntranceCounter:" + doc.appEntranceCounter);
                }
            });

            console.log("find photo:" + JSON.stringify(doc));
            res.json(doc);
            return;
         });*/

  /*      ,
    */